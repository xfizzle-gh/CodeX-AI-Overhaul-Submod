name: Allied Support Terrain FoW formatting validation

on:
  pull_request:

permissions:
  contents: write

jobs:
  repair-once:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR head
        uses: actions/checkout@v6
        with:
          ref: fix/allied-support-terrain-fow
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Repair generated block
        run: python tools/repair_terrain_fow_block.py

      - name: Run focused FoW regression suite
        run: python -m unittest tests/test_allied_support_shared_fow.py

      - name: Commit canonical source changes
        run: |
          git show origin/main:.github/workflows/allied-support-fow-guard.yml > .github/workflows/allied-support-fow-guard.yml
          rm tools/repair_terrain_fow_block.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix terrain FoW handoff formatting"
          git push origin HEAD:fix/allied-support-terrain-fow
