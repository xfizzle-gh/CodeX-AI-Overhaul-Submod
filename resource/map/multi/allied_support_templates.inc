; Empty Human "" shells were removed.
; Assigning a player ID does not populate a breed, model, inventory, or usable soldier.
; The test support system now clones real, equipped live cmp_def infantry instead.
; Keep this include as a harmless compatibility stub while CWA mission files still reference it.
